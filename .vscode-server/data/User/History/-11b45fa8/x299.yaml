apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project2-claim
spec:
  storageClassName: default-storage-class-davineves
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      namespace: davineves