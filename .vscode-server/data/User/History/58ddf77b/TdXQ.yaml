apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: davineves
spec:
  containers:
    - name: test-container
      image: nginx
      volumeMounts:
        - mountPath: /data
          name: project2-storage
  volumes:
    - name: project2-storage
      persistentVolumeClaim:
        claimName: project2-claim
